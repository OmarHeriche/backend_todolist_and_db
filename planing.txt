                        setting the setup :
1) setting the package.json  :: Done
2) dependecies:                 ::Done
        express,dotenv,,"nodemon";Done but mzali ghir :     mongoose
                        start developpement:
1) setting the easiest server logique:
    app,port,routers,controllers;
2) dealing the mongo db and get the connection string;
3) hide the connection string;
4) hide nodeModuels (in git ignore);
5) set the final logic ;
6) set the meaddlewears for the app.js;
        express.json,use(routers),use(ki naglat:));

the main route :
    /api/v1/tasks/
